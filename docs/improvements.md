# Improvements

Core's declared `.venv` commands made the missing-virtual-environment prerequisite visible before product work. Keep those commands project-native rather than adding a Castra wrapper.
