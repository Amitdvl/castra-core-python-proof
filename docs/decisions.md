# Decisions

The data file remains the source of truth for release work. `--doctor` exits 2 for missing data and names `RELEASE_DATA`, so setup/environment failures remain distinct from product failures.
