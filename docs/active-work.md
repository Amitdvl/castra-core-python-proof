# Active work

## Current

Core v2 proof is complete. The latest focused product change is the `--open` view in `release_board/__main__.py`, covered by `tests/test_app.py`.

## Next session

Start at `AGENTS.md`, then run the declared validation and inspection commands before extending a view. The ordinary missing-data reproduction is `RELEASE_DATA=<missing path> castra exercise --repo . troubleshoot`; remove the override to recover.
